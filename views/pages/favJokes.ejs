<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <p>hello</p>
    <% data.forEach(item=>{ %>
    <div>
        <p><%= item.id%></p>
        <p><%= item.type%></p>
        <p><%= item.setup%></p>
        <p><%= item.punchline%></p>
        <form action="/joke/<%= item.idp%>">
            <button>View-Details</button>
        </form>

    </div>


    <% })%>
</body>

</html>